import React from 'react';

import {
    InfoContainer, MoreInfo,
} from './styled';

export const Main = () => {
    return (
        <InfoContainer>
            <MoreInfo>More Info</MoreInfo>
        </InfoContainer>
    );
};

export default Main;
